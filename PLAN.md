# 김프 아비트라지 봇 — 마스터 플랜

> **상태: 브레인스토밍 진행 중.** 이 문서는 살아있는 문서다. 논제가 결론 나면 §7에서 확정 섹션으로 승격시키고, 새 논제는 §7에 추가한다.
>
> 최종 갱신: 2026-08-01 (v2 — T1 자본 배분, T2 헤지, 보안 아키텍처 반영)

---

## 1. 확정된 결정

| 항목 | 결정 |
|---|---|
| 전략 | **C. 실물 전송 김프 차익거래, 양방향** (김프·역프 모두. §1.1 배분 모델) |
| 언어 | **Python** (asyncio 기반 수집·실행, pandas/polars 분석) |
| 거래소 | 국내: 업비트, 빗썸 / 해외: 바이낸스, 바이비트, OKX / +DEX (솔라나·이더·BNB 체인 개인지갑) |
| 자본 | 테스트: 총 ~$30k → 최종 $1M+ 목표 |
| 시세 요건 | 탐지 지연 <100ms. **WebSocket 푸시 기반** (폴링 아님) |
| 관제 | **텔레그램 = 관제탑.** 전 과정 알림 + 단계별 킬스위치(L1/L2/L3) + 데드맨 스위치 |
| 보안 | **"뚫려도 훔쳐갈 수 없는 구조"가 1번 요구사항** (§4). 과거 수동 운용의 이유였던 출금 권한 문제를 설계로 해소 |
| 운용자 배경 | 장기간 수동 재정거래 경험 보유. 봇은 그 프로세스의 자동화 + 감독 모드로 시작 |

### 1.1 자본 배분 모델 (T1 확정)

- **양방향 봇.** "역프 코인이 없으면 출금하지 않는다"가 아니라 **모든 거래소를 목표 비율로 상시 복원한다.**
- 목표 비율 (유동적 조정 가능):

| 업비트 | 빗썸 | 바이낸스 | 바이비트 | OKX | DEX 지갑 |
|---|---|---|---|---|---|
| 2 | 2 | 3 | 1 | 1 | 1 |

- 예: 김프 사이클로 바이낸스→업비트로 가치가 이동해 업비트에 원화가 쌓이면, 그만큼 USDT로 바이낸스에 되돌려 놓는다. **같은 기회가 다시 왔을 때 즉시 동작 가능한 상태**가 불변식.
- 되돌림(리밸런싱) 실행 정책은 미결 → **O2** (즉시 복원 vs 밴드+타임아웃 넷팅)
- 헤지 증거금은 위 10유닛과 **별도 풀**로 상시 보유 (§1.2)
- DEX 지갑 유닛은 해외 CEX와만 연결 (국내 거래소↔개인지갑은 트래블룰 제약 — V5)
- **트리거 지표 = 실행 김프** (§2). 분모가 USDT/KRW 실거래가이므로 "되돌림 레그에서 USDT 김프를 되뱉는 비용"이 트리거에 자동 반영됨. 코인 김프 7% − USDT 김프 3% ≈ 실행 김프 3.9% = 왕복 후 실마진. 절대 김프 레벨과 무관하게 정합.

### 1.2 헤지 정책 (T2 — 방향별)

- **역프 방향 (국내 매수 → 해외로 전송 → 해외 매도): 헤지 확정.** 업비트 매수와 동시에 해외 무기한 숏 → 매도 거래소 = 헤지 거래소이므로 **스프레드 완전 락**. 잔여 리스크는 펀딩비 + 베이시스뿐.
- **김프 방향 (해외 매수 → 국내로 전송 → 국내 매도): 미결 → O1.** 헤지해도 글로벌 가격만 락되고 김프 변동분은 남지만, naked보다 리스크가 한 자릿수 작음. 후보 정책: "예상 전송시간 > N분 또는 금액 > $X면 헤지 필수"의 조건부 헤지.
- 헤지 증거금 사이징: `증거금 ≥ (동시 in-flight 상한 / 레버리지) × 버퍼2배`. 레버리지 3~5x 가정 시 in-flight $10k당 $4~7k.
- 펀딩비: 사이클 <1h면 통상 미미하나 알트 펀딩 스파이크(±0.1~0.3%/8h) 존재 → 진입 전 체크 항목에 포함.
- 선물이 없는 코인은 naked 한도 내에서만 취급 (T3 유니버스 필터로 연결).

### 1.3 텔레그램 관제 설계 원칙 (확정)

- 심각도 계층: INFO(묶음 전송) / WARN / CRIT(즉시 푸시)
- 킬스위치 단계: **L1** 신규 진입 중단(진행 중 사이클 완주) / **L2** L1 + 헤지 유지 / **L3** 전량 청산 후 플랫
- 블록체인 전송은 중간 정지 불가 → "정지"의 의미는 위 3단계로만 정의
- chat_id 화이트리스트. 텔레그램에는 정지·승인 권한만, 출금 발의 권한은 부여하지 않음
- 봇 → 텔레그램 하트비트 (끊기면 그 자체가 경보)
- 텔레그램은 감시·정지·승인 전용. 거래 루프에는 넣지 않음 (지연 1~3초)

### 1.4 레이턴시 설계 원칙 (확정)

- 시세: 전 거래소 WS 푸시 (업비트/빗썸 즉시, 바이낸스 depth@100ms, 바이비트/OKX 10~50ms) → 탐지 <100ms 달성
- rate limit은 REST(주문·출금·잔고)에만 해당. 시세 스트림 수신은 무제한
- 병목은 탐지가 아니라 ①주문 REST 왕복(50~300ms) ②전송 구간(2~30분, §1.2 헤지로 대응)
- 금액 연산은 전부 `Decimal`. float 금지
- 시각 이중 기록: 거래소 타임스탬프 + 로컬 수신 시각

---

## 2. 핵심 수식

```
이론 김프    = 국내가_KRW / (해외가_USDT × USD/KRW 고시환율) − 1     ← 분석·시장상황용
실행 김프    = 국내가_KRW / (해외가_USDT × USDT/KRW 실거래가) − 1    ← 트리거·손익계산용 (§1.1)

실현손익/사이클 = 실행 김프(진입 시점, 호가 깊이 반영 VWAP 기준)
              − 해외 taker 수수료 − 국내 taker 수수료
              − 출금 수수료 − 전송 중 김프 변동(O1 정책에 따름) − 펀딩비(헤지 시)
              − 리밸런싱 비용(O2 정책에 따름)
```

- 두 김프는 **처음부터 별도 필드로 수집·저장** (나중에 소급 불가)
- 슬리피지는 최우선 호가가 아니라 **호가 깊이 기반 VWAP**로 계산 (금액대별: 예 $5k / $50k / $500k)

---

## 3. 확인된 외부 제약 (2026-08-01 조사)

### 규제
- **외국환거래법 개정안 2026-05-07 국회 통과** — 가상자산 국경간 이전업무 등록제. 공포 후 6개월 시행(≈2026-11월). 해외 거래소 우회 거래·스테이블코인 해외 정산·OTC까지 잠재 규제 대상 언급. 개인 자가 차익거래의 해당 여부는 미확정 → **시행령 모니터링 + 실거래 규모 확대 전 전문가 확인 필요**
- 연간 해외송금 한도 $50k (외국환거래법)
- 사용자 인지 후 진행 결정됨 (2026-08-01)

### 트래블룰 / 출금
- **업비트 API 출금은 사전 등록 화이트리스트 주소로만 가능.** 주소 등록은 웹 UI 수동, 등록 시 상대 거래소 검증. 등록 후 API 자동화 가능 → **코인 유니버스는 사전 신고제. (코인, 네트워크, 목적지) 조합별 사전 등록 필수.** 이 제약은 보안 관점에서는 자산이 됨 (§4 방어선 1)
- 업비트: 미신고 거래소 23곳(쿠코인·MEXC 등) 출금 차단. 바이낸스·바이비트·OKX는 연동 대상 → 실측 확인 필요
- 빗썸: 화이트리스트 거래소만 입출금 가능. **API 트래블룰 동의 처리 방식 미확인 (실측 확인 항목 #V1)**

### 원화 입금 → 출금 지연 (업비트·빗썸 공통)
- 첫 원화 입금: 72시간 가상자산 출금 제한
- 이후 매 원화 입금 건: 해당 금액만큼 24시간 출금 제한
- → **은행 입금으로 루프를 돌릴 수 없음. 시드는 미리 숙성, 이후 거래소 내 회전**
- 코인 매도로 생긴 원화가 이 제한에 걸리는지 → **실측 확인 항목 #V2**

### Rate Limit
- 업비트: 그룹별 상한. Origin 헤더 포함 시 10초당 1회로 급감 → 서버사이드 필수
- 빗썸: WS 연결 IP당 초당 10회 (연결 후 수신은 무제한)
- 클라이언트 레이어에 토큰 버킷 내장

### 실측 확인 항목 (페이퍼/소액 단계 체크리스트)
- [ ] **V1**: 빗썸 API 출금 시 트래블룰 동의 파라미터 처리 방식
- [ ] **V2**: 코인 매도 원화가 24시간 출금 지연에 걸리는지
- [ ] **V3**: 업비트/빗썸 → 바이낸스·바이비트·OKX 화이트리스트 등록 실제 소요 시간·절차
- [ ] **V4**: 각 거래소 실효 입금 확정 시간 (요구 확정 블록 수 기준, 코인별)
- [ ] **V5**: 국내 거래소의 개인 지갑(DEX용) 화이트리스트 등록 가능 여부

---

## 4. 보안 아키텍처 — "뚫려도 훔쳐갈 수 없는 구조"

> 과거 수동 운용의 이유 = 봇 서버에 출금 권한을 주는 공포. 이 섹션의 목표는 **서버가 완전히 장악당해도 자산이 외부로 유출될 수 없는 구조**를 만들어 그 공포를 설계로 해소하는 것.

### 4.1 CEX 사이드 — 5중 방어

**방어선 0 — 서버에 없는 것들.** 거래소 로그인 비밀번호, OTP 시드, 화이트리스트 변경 권한은 서버에 존재하지 않는다. 서버에는 API 키만 있다. 화이트리스트 변경은 웹+2FA 수동 운영 절차로만 (의도적으로 자동화하지 않음).

**방어선 1 — 거래소단 주소 화이트리스트 (근본 방어선).** 한국 거래소는 트래블룰이 강제하고, 바이낸스·바이비트·OKX도 전부 지원 → 전 계정 활성화. 효과: **API 키를 통째로 탈취당해도 자금은 내 계정들 사이에서만 이동 가능.** 공격자 주소로의 출금은 거래소가 거부한다. 화이트리스트 변경은 API로 불가(수동+2FA+통상 지연 시간).

**방어선 2 — IP 바인딩.** 5개 거래소 전부 API 키 IP 화이트리스트 지원. 키는 봇 서버 고정 IP에서만 유효 → 키만 유출된 경우 무용지물.

**방어선 3 — 키 권한 3계층 분리.**
| 계층 | 권한 | 위치 |
|---|---|---|
| 시세 키 | 조회만 | 수집기 프로세스 |
| 거래 키 | 주문만 (출금 불가) | 실행 엔진 프로세스 |
| 출금 키 | 출금 | **출금 게이트웨이 전용** (격리 프로세스/컨테이너, 전략 코드에서 접근 불가) |

**방어선 4 — 출금 게이트웨이 정책 엔진.** 전략 엔진은 출금 API를 직접 호출하지 않고 내부 인증 큐로 `{코인, 수량, 목적지 거래소}` 요청만 보낸다. 게이트웨이가 검증: ①목적지가 내부 allowlist에 있고 거래소 등록 주소와 1:1 일치 ②건당/일일 한도 이내 ③이상 패턴(미등록 조합, 빈도 급증) 거부 ④전 건 텔레그램 CRIT 통지.

**방어선 5 — 감독 모드 → 자율 졸업.** 임계 금액 이상 출금은 텔레그램 인라인 버튼 승인 필요. **P3 초기값: 전 건 승인** — 지금까지의 수동 운용과 동일한 통제감으로 시작해서, 무사고 이력이 쌓이면 임계값을 올려 자율 범위를 넓힌다. 신뢰를 데이터로 사는 구조.

**잔여 리스크 (정직한 평가).** 서버 완전 장악 시 공격자의 최악 수단은 출금이 아니라 **악성 거래** (얇은 호가에 시장가를 던져 공격자 반대 주문에 체결시키는 가치 유출). 대응: 거래 키에도 봇 레벨 주문 상한 + 단시간 거래량 서킷브레이커, 이상 체결 텔레그램 CRIT. → CEX 사이드 최악 시나리오는 "전액 탈취"가 아니라 "한도 내 손실"로 축소됨.

### 4.2 DEX 사이드 — 진짜 약한 고리

온체인에는 화이트리스트가 없다. **핫키 유출 = 해당 지갑 전액 손실.** 이것이 전체 시스템에서 유일하게 '전액 탈취'가 가능한 지점.

- 배분 1유닛(총자본의 1/10) 상한 자체가 1차 방어 — 최악 손실 = 전체의 10%
- 체인별 지갑 분리 (솔라나 / 이더 / BNB 각각 별도 키) — 단일 키 유출의 반경 축소
- 토큰 approve 최소화 (per-trade 또는 exact amount), 정기 revoke
- MEV 보호 RPC (프라이빗 멤풀) 사용
- $1M 단계 진입 시: 서명 격리 (별도 서명 데몬 → MPC/HSM 계열) 도입 검토 — **T17로 논제화**

### 4.3 서버·코드 하드닝

- 전용 VM, SSH 키 온리, 인바운드 포트 최소화 (이상적으로 0 — 아웃바운드 전용, 텔레그램도 폴링/아웃바운드 웹훅)
- 시크릿: KMS 또는 SOPS+age. 저장소·로그에 시크릿 절대 금지 (pre-commit 훅 + 로그 마스킹)
- 파이썬 종속성 고정 + 해시 검증 (`--require-hashes`) — 공급망 공격 대비
- 감사 로그: 모든 주문·출금 요청/응답 append-only 기록

---

## 5. 아키텍처 스케치

```
┌────────────────────────────────────────────────────────────┐
│ Collectors (거래소별 독립 프로세스 — 장애 격리) [시세 키]        │
│ 업비트WS │ 빗썸WS │ 바이낸스WS │ 바이비트WS │ OKX WS │ 환율     │
└──────────────────────┬─────────────────────────────────────┘
                       │ 정규화 이벤트 (인메모리 핫패스)
┌──────────────────────▼─────────────────────────────────────┐
│ Normalizer — 심볼·네트워크 매핑, 시간 정렬                      │
└──────────┬──────────────────────────────┬──────────────────┘
┌──────────▼──────────┐        ┌──────────▼──────────────────┐
│ Spread Engine       │        │ Storage (콜드패스)            │
│ 이론/실행 김프 이원    │        │ TimescaleDB + Parquet        │
│ 깊이 기반 VWAP       │        │ 호가 상위 15단 스냅샷           │
└──────────┬──────────┘        └─────────────────────────────┘
┌──────────▼─────────────────────────────────────────────────┐
│ Opportunity Filter — T4 함정 탐지 게이트                      │
│ (지갑 상태, 급등 원인 분류, 유동성 체크)                         │
└──────────┬─────────────────────────────────────────────────┘
┌──────────▼─────────────────────────────────────────────────┐
│ Cycle Orchestrator — T6 상태머신 (영속화, 재기동 복구) [거래 키] │
│ 매수→헤지→출금요청→확정→입금확인→매도→헤지해제→정산               │
│ ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐   │
│ │ Risk Limits  │ │ Ledger 복식부기│ │ Rebalancer (§1.1)  │   │
│ │ T13          │ │ T14          │ │ 목표배분 감시·복원     │   │
│ └──────────────┘ └──────────────┘ └─────────┬──────────┘   │
└──────────┬─────────────────────────────────┼───────────────┘
           │ 내부 인증 큐 (출금 요청)           │
┌──────────▼─────────────────────────────────▼───────────────┐
│ Withdrawal Gateway (격리 프로세스) [출금 키] — §4.1 방어선 3~5  │
│ allowlist 대조 │ 한도 │ 이상 탐지 │ 텔레그램 승인(감독 모드)      │
└──────────┬─────────────────────────────────────────────────┘
┌──────────▼─────────────────────────────────────────────────┐
│ Telegram Control Tower — 알림 계층 + L1/L2/L3 킬스위치        │
│ + 출금 승인 버튼 + 하트비트/데드맨                              │
└────────────────────────────────────────────────────────────┘
```

원칙: Executor는 인터페이스로 정의하고 Paper 구현부터. Live 전환은 어댑터 교체.

---

## 6. 페이즈 로드맵

| 페이즈 | 내용 | 종료 조건 |
|---|---|---|
| **P0** | 수집기 + 저장 + 이원 김프 엔진 + 대시보드/텔레그램 알림 | 2주+ 데이터 축적, 기회 통계 확보 |
| **P1** | 백테스트: 기회 지속시간 분포, 전송창 김프 변동 분포, 금액대별 실효 스프레드 | "비용 빼고 남는가" 정량 답 |
| **P2** | 페이퍼 트레이딩 (전체 상태머신, V1~V5 해소, 화이트리스트 등록 완료) | 시뮬 손익 양(+), 운영 안정 |
| **P3** | 소액 실거래 ($30k, **출금 전 건 텔레그램 승인 모드**, 하드 리스크 한도) | N사이클 무사고 + 실현 수익 |
| **P4** | 자율도 상향 + 스케일업 (T15, T17 해소 후) | — |

브레인스토밍과 병행하여 P0 구현은 즉시 착수 가능 (어떤 미결 논제와도 독립).

---

## 7. 미결 논제 (브레인스토밍 큐)

★ = 이게 틀리면 나머지가 무의미 / ✅ = 확정되어 본문 승격

### 열린 질문 (확정 논제의 세부)
- **O1. 김프 방향 헤지**: naked vs 조건부 헤지("전송 예상 > N분 or 금액 > $X면 헤지"). §1.2
- **O2. 리밸런싱 실행 정책**: 사이클 직후 즉시 복원 vs 밴드+타임아웃 넷팅 (반대 방향 흐름 상계로 전송비 절약). §1.1
- **O3. DEX 유닛 구성**: 체인별(솔라나/이더/BNB) 배분, 가스용 네이티브 vs 스테이블 비율
- **O4. 헤지 증거금 규모**: 동시 in-flight 상한이 정해지면(T13) 도출

### 전략 코어
- ~~★ T1. 루프 폐쇄~~ → ✅ §1.1 목표 배분 상시 복원 모델
- ~~★ T2. 전송 레그 헤지~~ → ✅ §1.2 (역프 확정, 김프 방향은 O1)
- **★ T3. 코인 유니버스 매트릭스**: 레일 코인(XRP·TRX·SOL) vs 기회 코인(알트). 전송속도·출금비·유동성·**선물유무(§1.2 전제)**·화이트리스트 가능 여부. 사전 등록제이므로 유니버스 확정이 P2 선행 조건.
- **★ T4. 함정 탐지**: 김프 급등의 흔한 원인 = 입금 중단. 진입 전 지갑 상태 게이트, 급등 원인 분류, 사이클 중 출금 중단 대응 절차.
- **T5. DEX 역할**: 해외 레그 조달? 우회 경로? 브릿지? MEV·가스 비용 모델. (보안은 §4.2로 확정, 역할 정의가 미결)

### 실행 계층
- **★ T6. 사이클 상태머신·장애 복구**: 단계 영속화, 재기동 이어가기, 멱등성, 잔고 대조, 입금 미확인 타임아웃 정책.
- **T7. 실행 전술**: IOC vs 지정가, 최대 슬리피지 가드, 부분 체결 시 헤지 동기화, 레그 실패 롤백.
- **T8. 고시환율 소스 선정** (이원 김프 구조는 §2로 확정)

### 인프라 계층
- **T9. 배치**: AWS 서울 + 도쿄? 고정 IP(API 화이트리스트), 해외 거래소 클라우드 IP 정책.
- **T10. 데이터 계층**: 인메모리 핫패스 / TimescaleDB+Parquet 콜드패스, 호가 15단.
- ~~T11. 키 보안~~ → ✅ §4 보안 아키텍처
- **T12. 관측성**: NTP 동기화, 스트림 신선도 감시, Prometheus/Grafana.
- **T17. 서명 격리 (신규)**: $1M 단계의 DEX 키 관리 — 서명 데몬 분리, MPC/HSM 검토. §4.2

### 운영·리스크 계층
- **★ T13. 리스크 한도**: 사이클당 상한, **동시 in-flight 총액 상한**(→O4 연동), 코인당 상한, 일손실 자동 L1, 연속 실패 자동 정지. 테스트 단계 수치 정하기.
- **T14. 회계 원장**: 복식부기, 사이클별 손익 분해(스프레드/펀딩/수수료/슬리피지/환율), 세무 원천 자료.
- **T15. 확장 시 깨지는 것**: 일일 출금 한도, 알트 깊이 대비 주문 규모, 다계좌·법인·세무 구조.
- **T16. 백테스트 시뮬레이터**: P0 데이터로 헤지 기대효과·기회 분포 정량화.

---

## 8. 리스크 레지스터

| 리스크 | 심각도 | 대응 |
|---|---|---|
| 전송 중 김프 급락 | 높음 | §1.2 헤지 (역프 완전 락, 김프 방향 O1) |
| 사이클 중 입출금 중단 | 높음 | T4 게이트 + 중단 시 대응 절차 |
| 외국환거래법 개정 시행 (≈11월) | 높음 | 시행령 모니터링, 확대 전 전문가 확인 |
| 봇 크래시 중 자산 in-flight | 높음 | T6 영속 상태머신 + 데드맨 경보 |
| DEX 핫키 유출 (전액 탈취 가능 유일 지점) | 높음 | §4.2 배분 상한·키 분리·approve 최소화, T17 |
| 서버 장악 → 악성 거래로 가치 유출 | 중간 | §4.1 잔여 리스크 대응 (주문 상한·서킷브레이커) |
| 스트림 조용한 죽음 (stale 시세로 판단) | 중간 | T12 신선도 감시, stale 시 진입 차단 |
| API 키 유출 | 낮음 (방어 후) | §4.1 5중 방어 — 화이트리스트가 최종 방어선 |
| USDT 디페그 | 낮음 | 실행 김프 기준 계산이라 부분 흡수, 극단 시 L2 |
