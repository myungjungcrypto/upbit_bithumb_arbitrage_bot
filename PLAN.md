# 김프 아비트라지 봇 — 마스터 플랜

> **상태: 브레인스토밍 진행 중.** 이 문서는 살아있는 문서다. 논제가 결론 나면 §7에서 확정 섹션으로 승격시키고, 새 논제는 §7에 추가한다.
>
> 최종 갱신: 2026-08-02 (v9 — 가동 첫날 피드백 반영: 알림 정책 재설계·지갑 연동 함정 태깅·FX 폴백)

---

## 1. 확정된 결정

| 항목 | 결정 |
|---|---|
| 전략 | **C. 실물 전송 김프 차익거래, 양방향** (김프·역프 모두. §1.1 배분 모델) |
| 언어 | **Python** (asyncio 기반 수집·실행, pandas/polars 분석) |
| 거래소 | 국내: 업비트, 빗썸 / 해외: 바이낸스, 바이비트, OKX / +DEX (솔라나·이더·BNB 체인 개인지갑) |
| 자본 | 테스트: 총 ~$30k → 최종 $1M+ 목표 |
| 시세 요건 | 탐지 지연 <100ms. **WebSocket 푸시 기반** (폴링 아님) |
| 관제 | **텔레그램 = 관제탑.** 전 과정 알림 + 단계별 킬스위치(L1/L2/L3) + 데드맨 스위치 |
| 보안 | **"뚫려도 훔쳐갈 수 없는 구조"가 1번 요구사항** (§4). 과거 수동 운용의 이유였던 출금 권한 문제를 설계로 해소 |
| 운용자 배경 | 장기간 수동 재정거래 경험 보유. 봇은 그 프로세스의 자동화 + 감독 모드로 시작 |

### 1.1 자본 배분 모델 (T1 확정)

- **양방향 봇.** "역프 코인이 없으면 출금하지 않는다"가 아니라 **모든 거래소를 목표 비율로 상시 복원한다.**
- 목표 비율 (유동적 조정 가능):

| 업비트 | 빗썸 | 바이낸스 | 바이비트 | OKX | DEX 지갑 |
|---|---|---|---|---|---|
| 2 | 2 | 3 | 1 | 1 | 1 |

- 예: 김프 사이클로 바이낸스→업비트로 가치가 이동해 업비트에 원화가 쌓이면, 그만큼 USDT로 바이낸스에 되돌려 놓는다. **같은 기회가 다시 왔을 때 즉시 동작 가능한 상태**가 불변식.
- **되돌림(리밸런싱) 실행 정책 확정 (O2 ✅): 기회 주도 복귀 레그 + 밴드/타임아웃.**
  - 원리: 손익은 더해지고, **전송이 상쇄된다.** 복귀 레그의 원천은 "절대 김프 ± 동시 발생"이 아니라 **코인별 실행 김프의 분산** — USDT 김프보다 프리미엄이 낮은 코인은 절대 김프가 +여도 실행 김프가 음수(실질 역프)이며, 그 코인의 outbound 거래가 그 자체로 수익이자 리밸런싱임
  - 정합성: inbound 엣지 + outbound 엣지 ≈ 두 코인 프리미엄 차이. 양방향이 모두 체결되면 USDT 복귀 레그가 물리적으로 불필요해짐
  - 정책: 편차가 밴드(기본 ±20%p) 이내면 타임아웃(기본 15분)까지 반대 방향 기회를 대기 → 기회가 오면 그 거래로 복원, 없으면 **그 시점 최저 프리미엄 자산(통상 USDT)으로 강제 복귀**
  - `타임아웃=0` 설정 시 즉시 복원 모드와 동일 (운용자 원안). **기본 파라미터는 P1 데이터(반대 기회 도착 간격 분포)로 확정**
  - 김프 급등 국면에는 전 코인 프리미엄이 동반 상승해 반대 기회가 소멸 → 밴드 이탈 → 자동으로 즉시 복귀로 퇴화 (수동 운용 직관과 일치)
- 헤지 증거금은 위 10유닛과 **별도 풀**로 상시 보유 (§1.2)
- DEX 지갑 유닛은 해외 CEX와만 연결 (국내 거래소↔개인지갑은 트래블룰 제약 — V5)
- **트리거 지표 = 실행 김프** (§2). 분모가 USDT/KRW 실거래가이므로 "되돌림 레그에서 USDT 김프를 되뱉는 비용"이 트리거에 자동 반영됨. 코인 김프 7% − USDT 김프 3% ≈ 실행 김프 3.9% = 왕복 후 실마진. 절대 김프 레벨과 무관하게 정합.

### 1.2 헤지 정책 (T2 — 방향별)

- **역프 방향 (국내 매수 → 해외로 전송 → 해외 매도): 확정.** 해외 무기한 있는 페어는 진입과 동시에 숏(매도 거래소 = 헤지 거래소 → **스프레드 완전 락**, 잔여는 펀딩+베이시스). 선물 없는 페어는 naked 허용 — 단 naked 전용 한도(T13) 이내.
- **김프 방향 (해외 매수 → 국내로 전송 → 국내 매도): 기본 naked 확정 (O1 ✅, 운용자 결정 2026-08-01)**
  - 사실관계: 국내에 숏 수단이 없어 '완전 락'은 구조적으로 불가. 해외 무기한 숏으로 부분 헤지(글로벌 가격 성분 제거)는 가능하나, **장기 반복 운용 기대값 관점에서 naked 채택** — 전송창 가격 변동은 ~대칭이라 평균적으로 상쇄되는 반면 헤지 비용(왕복 수수료 ~10bp + 펀딩 + 운영 복잡도)은 매 사이클 확정 지출
  - **naked 가드레일 3종**:
    1. naked in-flight 총액 상한은 헤지 모드 대비 보수적으로 설정 (T13에서 수치 확정)
    2. naked 사이클은 레일 코인(짧은 전송창) 우선 배정
    3. **재검토 트리거**: P0/P1에서 전송창 내 가격·김프 변동 분포를 실측 — 특히 **변동성 클러스터링**(기회 발생 시점 = 고변동 시점이면 "대칭 상쇄" 가정 약화) 여부. naked 실비용(변동 드래그+테일)이 헤지 비용을 넘으면 재론
  - 조건부 부분 헤지("전송 > N분 or 금액 > $X면 헤지")는 **config 파라미터로 구현하되 기본 off**. 헤지는 EV 개선 도구가 아니라 **동일 리스크 한도에서 사이즈를 키우는 도구** — $1M 스케일업(T15) 시 재평가
- 헤지 증거금 사이징: `증거금 ≥ (동시 in-flight 상한 / 레버리지) × 버퍼2배`. 레버리지 3~5x 가정 시 in-flight $10k당 $4~7k. 별도 풀 상시 보유 (확정).
- 펀딩비: 사이클 <1h면 통상 미미하나 알트 펀딩 스파이크(±0.1~0.3%/8h) 존재 → 진입 전 체크 항목에 포함.
- 선물 유무는 코인 유니버스 매트릭스(T3)의 1급 속성.

### 1.3 텔레그램 관제 설계 원칙 (확정)

- 심각도 계층: INFO(분당 예산 내 즉시, 초과분 60초 다이제스트) / WARN·CRIT(즉시 푸시)
- 알림은 판단을 담아야 함: 엣지 알림은 지갑 상태를 참조해 **"함정(입출금 정지 중)" / "비정상 괴리(±10%↑, 상태 미확인)" / 정상 기회**를 구분 표기 (2026-08-02, 가동 첫날 ZIL·STORJ 실증 반영)
- 기동 시 지갑 상태 첫 스냅샷은 개별 알림 금지 — 정지 중인 코인 요약 1건만
- 킬스위치 단계: **L1** 신규 진입 중단(진행 중 사이클 완주) / **L2** L1 + 헤지 유지 / **L3** 전량 청산 후 플랫
- 블록체인 전송은 중간 정지 불가 → "정지"의 의미는 위 3단계로만 정의
- chat_id 화이트리스트. 텔레그램에는 정지·승인 권한만, 출금 발의 권한은 부여하지 않음
- 봇 → 텔레그램 하트비트 (끊기면 그 자체가 경보)
- 텔레그램은 감시·정지·승인 전용. 거래 루프에는 넣지 않음 (지연 1~3초)

### 1.4 속도 원칙 — 운용자 제1원칙 (확정, 2026-08-02)

> **"가장 빨리 포착해서, 가장 빨리 매수하고, 가장 빨리 출금시키고, 가장 빨리 매도한다."**
>
> 김프는 전 세계 따리꾼과의 경주다. 프리미엄은 경쟁자들의 in-flight 물량이 도착하는 순서대로 소멸하며,
> 함정(T4-⑥)은 대열의 후미를 문다. **속도는 수익의 원천이자 그 자체로 방어다.**
> 따라서 진입 판단에 '시간을 기다리는' 게이트는 금지 — 모든 게이트는 이미 스트리밍된 데이터로 밀리초에 평가한다.

엔지니어링 함의 (전 단계 속도 예산):
- **포착**: 전 거래소 WS 푸시 (업비트/빗썸 즉시, 바이낸스 depth@100ms, 바이비트/OKX 10~50ms) → 탐지 <100ms ✅ (P0 구현)
- **매수**: 인증 REST 세션 상시 keep-alive (TLS 핸드셰이크를 임계 경로에서 제거), 사전 포지셔닝된 자본(§1.1의 존재 이유)
- **출금**: 매수 체결 확인 이벤트 → **즉시 출금 API 발사** (배치·직렬 지연 금지). 화이트리스트 사전 등록이라 셋업 0
- **체결·입금 감지**: 폴링이 아니라 **프라이빗 WS** (업비트 myOrder/myAsset, 바이낸스 user data stream 등) — 잔고 폴링 대비 수 초 절약, rate limit 무관
- **매도**: 입금 크레딧 이벤트 → 즉시 매도. 진입만큼 **탈출 속도**도 경주임 (경쟁자 물량과 같은 블록으로 도착한다)
- **측정**: 사이클 원장에 단계별 타임스탬프 → P1에서 가장 뚱뚱한 단계를 최적화. 통제 불가능 구간(거래소 출금 처리 대기, 체인 확정)은 실측해서 레일 선택에 반영(V4)
- **출금 처리 속도 실측 사전정보 (운용자, 2026-08-02)**: 최장 단계 = **출금 처리 대기**. 업비트·빗썸 느림 / 바이낸스·바이비트·OKX 빠름 / **DEX(자가수탁)는 즉시 브로드캐스트 = 최속**. 단 상황마다 변동 → 함의:
  ① **역프 방향(국내 출금)이 가장 느린 레그** — 역프 헤지 확정(§1.2)과 정합: 노출이 긴 방향에 이미 헤지가 걸려 있음
  ② **김프 방향(해외 출금)은 빠름** — naked 결정(O1)의 노출 시간이 실제로 짧은 방향임
  ③ **DEX 유닛 = 최속 레일 후보** — 인바운드에서 DEX 매수→국내 직접 입금이 열리면 CEX 출금 대기를 통째로 제거. **V5(개인지갑↔국내 거래소 입금 경로) 우선순위 상승**
  ④ 출금 처리시간은 (거래소, 코인, 시간대)별 **롤링 실측 모델**로 관리, 라우팅·타임아웃(T6)에 동적 반영. "상황마다 다름"을 감이 아니라 분포로
- **운용자 수동 프로세스의 2대 병목 = 봇 엣지의 본체**: ①출금 처리 대기(라우팅으로 최소화) ②머릿속 판단(원인 분류·입출금 확인·사이징 — 경험과 감) → 후자를 0-지연 게이트 + 신뢰 등급 + **호가 기반 capacity 사이징**(엔진이 "임계 엣지를 유지하는 최대 명목"을 직접 계산)으로 밀리초화. 물리적 전송 시간은 모두에게 평등하므로, 경쟁 우위는 탐지·판단·발사·라우팅의 속도에서 나온다
- **엣지 반감기 실측**: analyze_premium.py의 지속시간 분포 = 우리가 얼마나 빨라야 하는지의 목표치
- rate limit은 REST(주문·출금·잔고)에만 해당. 시세 스트림 수신은 무제한
- 금액 연산은 전부 `Decimal`. float 금지 / 시각 이중 기록: 거래소 타임스탬프 + 로컬 수신 시각

---

## 2. 핵심 수식

```
이론 김프    = 국내가_KRW / (해외가_USDT × USD/KRW 고시환율) − 1     ← 분석·시장상황용
실행 김프    = 국내가_KRW / (해외가_USDT × USDT/KRW 실거래가) − 1    ← 트리거·손익계산용 (§1.1)

실현손익/사이클 = 실행 김프(진입 시점, 호가 깊이 반영 VWAP 기준)
              − 해외 taker 수수료 − 국내 taker 수수료
              − 출금 수수료 − 전송 중 김프 변동(O1 정책에 따름) − 펀딩비(헤지 시)
              − 리밸런싱 비용(O2 정책에 따름)
```

- 두 김프는 **처음부터 별도 필드로 수집·저장** (나중에 소급 불가)
- 슬리피지는 최우선 호가가 아니라 **호가 깊이 기반 VWAP**로 계산 (금액대별: 예 $5k / $50k / $500k)
- **출금 수수료는 정액+정률의 합** (운용자 실측 2026-08-28): 빗썸 알트 다수가 명목의 ~1% 정률 → OUT 사이클의 국내 코인 출금에 거래소·코인별 정률 가산. 실효 손익분기: **빗썸 OUT ≈ 총엣지 1.5%+** (순엣지 0.5% 임계 기준). 코인별 실값 표는 P3 전 입력 (VERIFY)

---

## 3. 확인된 외부 제약 (2026-08-01 조사)

### 규제
- **외국환거래법 개정안 2026-05-07 국회 통과** — 가상자산 국경간 이전업무 등록제. 공포 후 6개월 시행(≈2026-11월). 해외 거래소 우회 거래·스테이블코인 해외 정산·OTC까지 잠재 규제 대상 언급. 개인 자가 차익거래의 해당 여부는 미확정 → **시행령 모니터링 + 실거래 규모 확대 전 전문가 확인 필요**
- 연간 해외송금 한도 $50k (외국환거래법)
- 사용자 인지 후 진행 결정됨 (2026-08-01)

### 트래블룰 / 출금
- **업비트 API 출금은 사전 등록 화이트리스트 주소로만 가능.** 주소 등록은 웹 UI 수동, 등록 시 상대 거래소 검증. 등록 후 API 자동화 가능 → **코인 유니버스는 사전 신고제. (코인, 네트워크, 목적지) 조합별 사전 등록 필수.** 이 제약은 보안 관점에서는 자산이 됨 (§4 방어선 1)
- 업비트: 미신고 거래소 23곳(쿠코인·MEXC 등) 출금 차단. 바이낸스·바이비트·OKX는 연동 대상 → 실측 확인 필요
- 빗썸: 화이트리스트 거래소만 입출금 가능. **API 트래블룰 동의 처리 방식 미확인 (실측 확인 항목 #V1)**

### 원화 입금 → 출금 지연 (업비트·빗썸 공통)
- 첫 원화 입금: 72시간 가상자산 출금 제한
- 이후 매 원화 입금 건: 해당 금액만큼 24시간 출금 제한
- → **은행 입금으로 루프를 돌릴 수 없음. 시드는 미리 숙성, 이후 거래소 내 회전**
- 코인 매도로 생긴 원화가 이 제한에 걸리는지 → **실측 확인 항목 #V2**

### Rate Limit
- 업비트: 그룹별 상한. Origin 헤더 포함 시 10초당 1회로 급감 → 서버사이드 필수
- 빗썸: WS 연결 IP당 초당 10회 (연결 후 수신은 무제한)
- 클라이언트 레이어에 토큰 버킷 내장

### 실측 확인 항목 (페이퍼/소액 단계 체크리스트)
- [ ] **V1**: 빗썸 API 출금 시 트래블룰 동의 파라미터 처리 방식
- [ ] **V2**: 코인 매도 원화가 24시간 출금 지연에 걸리는지
- [ ] **V3**: 업비트/빗썸 → 바이낸스·바이비트·OKX 화이트리스트 등록 실제 소요 시간·절차
- [ ] **V4**: 각 거래소 실효 입금 확정 시간 (요구 확정 블록 수 기준, 코인별)
- [ ] **V5**: 국내 거래소의 개인 지갑(DEX용) 화이트리스트 등록 가능 여부 — **입금 방향 포함** (개인지갑→업비트/빗썸 입금이 열리면 인바운드에서 CEX 출금 대기 제거 = 최속 레일. §1.4 함의 ③, 우선순위 상승)
- [ ] **V6**: 거래소별 출금 신청 후 취소 가능 여부·조건 — 운용자 경험으로 1차 확인됨(빗썸 즉시 가능, 업비트 비결정적). API 레벨 동작(취소 엔드포인트·상태 코드)만 P2에서 재확인
- [x] **V8: 해외측 지갑 상태 연동** — ✅ 2026-08-28 가동 (바이낸스 read-only 키, capital/config 60s 폴링). 알림·시뮬레이션 게이트 모두 "IN=해외출금∧국내입금 / OUT=국내출금∧해외입금"으로 확장. 배경: 가상 수확 첫 실행(08-27)에서 이 사각+V7이 일 60% 허수 생성 → 요주의 격리(>5% 지속)로 8.9%까지 정화, V8 데이터 축적 후 재분석이 첫 청정 추정치 (예상 수렴 일 0.3~1%)
- [x] **V7: 심볼 충돌(자산 동일성) 검증** — ✅ 2026-08-28 전수 판정 완료 (199심볼: MISMATCH 1=AI, NO_NET_OVERLAP 12 → trade_blocklist 13종, OK 186) — **AI 충돌 확정 (2026-08-28 운용자 확인: 업비트 AI=젠신(Gensyn) ≠ 바이낸스 AI=Sleepless AI — market/all 1차 소스로 확정)** → trade_blocklist 등재·열린 페이퍼 사이클 소급 무효. 검증 자동화: `scripts/verify_universe.py` — 티커가 아니라 **CoinGecko 정식 자산 ID(컨트랙트 기준 매핑) 대조 + 네트워크 교집합 검사** (운용자 제안 반영: 컨트랙트 수준 대조가 정석, 거래소가 컨트랙트를 직접 공개하지 않으므로 애그리게이터 ID 경유). 전 유니버스 일괄 판정 → blocklist 자동 생성. P3 전 필수 1회 + 신규 상장 시 재실행 — 티커 문자열 매칭은 위험: 같은 티커가 거래소마다 다른 자산일 수 있음 (실측 사례: "AI"가 업비트·빗썸 모두에서 바이낸스 대비 +25% 지속 — 2026-08-04, 티커 충돌 의심). **거래 가능 유니버스 확정 전 코인별 풀네임·네트워크·컨트랙트 대조 필수** (업비트 market/all의 한글명 × 바이낸스 exchangeInfo × CoinGecko 교차). 지속 대괴리 + 양 국내 거래소 동일 패턴 = 충돌 1순위 의심 신호

---

## 4. 보안 아키텍처 — "뚫려도 훔쳐갈 수 없는 구조"

> 과거 수동 운용의 이유 = 봇 서버에 출금 권한을 주는 공포. 이 섹션의 목표는 **서버가 완전히 장악당해도 자산이 외부로 유출될 수 없는 구조**를 만들어 그 공포를 설계로 해소하는 것.

### 4.1 CEX 사이드 — 5중 방어

**방어선 0 — 서버에 없는 것들.** 거래소 로그인 비밀번호, OTP 시드, 화이트리스트 변경 권한은 서버에 존재하지 않는다. 서버에는 API 키만 있다. 화이트리스트 변경은 웹+2FA 수동 운영 절차로만 (의도적으로 자동화하지 않음).

**방어선 1 — 거래소단 주소 화이트리스트 (근본 방어선).** 한국 거래소는 트래블룰이 강제하고, 바이낸스·바이비트·OKX도 전부 지원 → 전 계정 활성화. 효과: **API 키를 통째로 탈취당해도 자금은 내 계정들 사이에서만 이동 가능.** 공격자 주소로의 출금은 거래소가 거부한다. 화이트리스트 변경은 API로 불가(수동+2FA+통상 지연 시간).

**방어선 1-b — 바이낸스 운영 정책 (2026-08-30 확정).** 화이트리스트는 계정 전역(웹+API 공통, 채널 분리 불가) → **거래소 주소만 등록, 개인지갑은 미등록.** 개인지갑 수동 출금은 임시 추가(2FA)→출금→삭제 절차로. One-step withdrawal OFF. 자금 구조: **볼트 서브(대부분 자금, API 없음, 외부 출금 자체 불가) / 트레이딩 서브($9k, 거래 전용 키) / 마스터(얇은 float + 게이트웨이 키 2종: 이체 전용·출금 전용 분리).** 상관 침해(서버 침해로 API키+DEX지갑키 동시 유출) 대응: 감독 모드 전 건 승인 + 마스터 얇게 + T17(DEX 키 서버 격리).

**방어선 2 — IP 바인딩.** 5개 거래소 전부 API 키 IP 화이트리스트 지원. 키는 봇 서버 고정 IP에서만 유효 → 키만 유출된 경우 무용지물.

**방어선 3 — 키 권한 3계층 분리.**
| 계층 | 권한 | 위치 |
|---|---|---|
| 시세 키 | 조회만 | 수집기 프로세스 |
| 거래 키 | 주문만 (출금 불가) | 실행 엔진 프로세스 |
| 출금 키 | 출금 | **출금 게이트웨이 전용** (격리 프로세스/컨테이너, 전략 코드에서 접근 불가) |

**방어선 4 — 출금 게이트웨이 정책 엔진.** 전략 엔진은 출금 API를 직접 호출하지 않고 내부 인증 큐로 `{코인, 수량, 목적지 거래소}` 요청만 보낸다. 게이트웨이가 검증: ①목적지가 내부 allowlist에 있고 거래소 등록 주소와 1:1 일치 ②건당/일일 한도 이내 ③이상 패턴(미등록 조합, 빈도 급증) 거부 ④전 건 텔레그램 CRIT 통지.

**방어선 5 — 감독 모드 → 자율 졸업 (확정, 2026-08-01).** 임계 금액 이상 출금은 텔레그램 인라인 버튼 승인 필요. **P3 초기값: 전 건 승인** — 지금까지의 수동 운용과 동일한 통제감으로 시작해서, 무사고 이력이 쌓이면 임계값을 올려 자율 범위를 넓힌다. 신뢰를 데이터로 사는 구조.

**잔여 리스크 (정직한 평가).** 서버 완전 장악 시 공격자의 최악 수단은 출금이 아니라 **악성 거래** (얇은 호가에 시장가를 던져 공격자 반대 주문에 체결시키는 가치 유출). 대응: 거래 키에도 봇 레벨 주문 상한 + 단시간 거래량 서킷브레이커, 이상 체결 텔레그램 CRIT. → CEX 사이드 최악 시나리오는 "전액 탈취"가 아니라 "한도 내 손실"로 축소됨.

### 4.2 DEX 사이드 — 진짜 약한 고리

온체인에는 화이트리스트가 없다. **핫키 유출 = 해당 지갑 전액 손실.** 이것이 전체 시스템에서 유일하게 '전액 탈취'가 가능한 지점.

- 배분 1유닛(총자본의 1/10) 상한 자체가 1차 방어 — 최악 손실 = 전체의 10%
- 체인별 지갑 분리 (솔라나 / 이더 / BNB 각각 별도 키, **배분 1:1:1 확정 — 필요시 조정**) — 단일 키 유출의 반경 축소
- 토큰 approve 최소화 (per-trade 또는 exact amount), 정기 revoke
- MEV 보호 RPC (프라이빗 멤풀) 사용
- $1M 단계 진입 시: 서명 격리 (별도 서명 데몬 → MPC/HSM 계열) 도입 검토 — **T17로 논제화**

### 4.3 서버·코드 하드닝

- 전용 VM, SSH 키 온리, 인바운드 포트 최소화 (이상적으로 0 — 아웃바운드 전용, 텔레그램도 폴링/아웃바운드 웹훅)
- 시크릿: KMS 또는 SOPS+age. 저장소·로그에 시크릿 절대 금지 (pre-commit 훅 + 로그 마스킹)
- 파이썬 종속성 고정 + 해시 검증 (`--require-hashes`) — 공급망 공격 대비
- 감사 로그: 모든 주문·출금 요청/응답 append-only 기록

---

## 5. 아키텍처 스케치

```
┌────────────────────────────────────────────────────────────┐
│ Collectors (거래소별 독립 프로세스 — 장애 격리) [시세 키]        │
│ 업비트WS │ 빗썸WS │ 바이낸스WS │ 바이비트WS │ OKX WS │ 환율     │
└──────────────────────┬─────────────────────────────────────┘
                       │ 정규화 이벤트 (인메모리 핫패스)
┌──────────────────────▼─────────────────────────────────────┐
│ Normalizer — 심볼·네트워크 매핑, 시간 정렬                      │
└──────────┬──────────────────────────────┬──────────────────┘
┌──────────▼──────────┐        ┌──────────▼──────────────────┐
│ Spread Engine       │        │ Storage (콜드패스)            │
│ 이론/실행 김프 이원    │        │ TimescaleDB + Parquet        │
│ 깊이 기반 VWAP       │        │ 호가 상위 15단 스냅샷           │
└──────────┬──────────┘        └─────────────────────────────┘
┌──────────▼─────────────────────────────────────────────────┐
│ Opportunity Filter — T4 함정 탐지 게이트                      │
│ (지갑 상태, 급등 원인 분류, 유동성 체크)                         │
└──────────┬─────────────────────────────────────────────────┘
┌──────────▼─────────────────────────────────────────────────┐
│ Cycle Orchestrator — T6 상태머신 (영속화, 재기동 복구) [거래 키] │
│ 매수→헤지→출금요청→확정→입금확인→매도→헤지해제→정산               │
│ ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐   │
│ │ Risk Limits  │ │ Ledger 복식부기│ │ Rebalancer (§1.1)  │   │
│ │ T13          │ │ T14          │ │ 목표배분 감시·복원     │   │
│ └──────────────┘ └──────────────┘ └─────────┬──────────┘   │
└──────────┬─────────────────────────────────┼───────────────┘
           │ 내부 인증 큐 (출금 요청)           │
┌──────────▼─────────────────────────────────▼───────────────┐
│ Withdrawal Gateway (격리 프로세스) [출금 키] — §4.1 방어선 3~5  │
│ allowlist 대조 │ 한도 │ 이상 탐지 │ 텔레그램 승인(감독 모드)      │
└──────────┬─────────────────────────────────────────────────┘
┌──────────▼─────────────────────────────────────────────────┐
│ Telegram Control Tower — 알림 계층 + L1/L2/L3 킬스위치        │
│ + 출금 승인 버튼 + 하트비트/데드맨                              │
└────────────────────────────────────────────────────────────┘
```

원칙: Executor는 인터페이스로 정의하고 Paper 구현부터. Live 전환은 어댑터 교체.

---

## 6. 페이즈 로드맵

| 페이즈 | 내용 | 종료 조건 |
|---|---|---|
| **P0** | 수집기 + 저장 + 이원 김프 엔진 + 대시보드/텔레그램 알림 **(2026-08-01 착수)** | 2주+ 데이터 축적, 기회 통계 확보 |
| **P1** | 백테스트: 기회 지속시간 분포, 전송창 김프 변동 분포, 금액대별 실효 스프레드 | "비용 빼고 남는가" 정량 답 |
| **P2** | 페이퍼 트레이딩 (전체 상태머신, V1~V5 해소, 화이트리스트 등록 완료) **(2026-08-28 착수 — T13 승인으로 미결 0)** | 시뮬 손익 양(+), 운영 안정 |
| **P3** | 소액 실거래 ($30k, **출금 전 건 텔레그램 승인 모드**, 하드 리스크 한도) | N사이클 무사고 + 실현 수익 |
| **P4** | 자율도 상향 + 스케일업 (T15, T17 해소 후) | — |

브레인스토밍과 병행하여 P0 구현은 즉시 착수 가능 (어떤 미결 논제와도 독립).

### 6.1 해외 다변화 M1–M3 (확정 2026-08-30) — 라이브는 OKX 우선

운용자 결정: **P3 첫 라이브는 바이낸스가 아니라 OKX부터.** OKX엔 비교적 적은 금액만 두므로
사고가 나도 손해가 격리된다(§4 방어선과 같은 철학 — 노출 최소화). 바이낸스는 자금이 커서
서브계좌 격리(§4.1-b)를 마친 뒤 후순위로 투입. 해외 관측은 단독(바낸)에서 3곳으로 확장한다.

| 단계 | 내용 | 상태 |
|---|---|---|
| **M1 관측 확장** | 김프 산출을 (국내 2 × 해외 3) 전 조합으로: 유니버스에 bybit/okx 상장분 교집합, 엔진 `ovs_ex` 차원, 저장·알림·페이퍼 수수료 레그 인지, OKX·바이비트 지갑 상태 수집기(읽기 전용 키), 분석 스크립트 레그 일반화(구 데이터는 binance로 소급 라벨) | ✅ 2026-08-30 |
| **M2 검증 확장** | verify_universe를 해외 3곳으로: CoinGecko 티커 대조(okx=okex, bybit_spot) + 레그별(국내×해외) 네트워크 교집합, blocklist 레그 문법 확장("COIN@dom>ovs") | 대기 |
| **M3 라이브 (OKX 우선)** | OKX 주문·출금·입금감지 어댑터 → 출금 게이트웨이 연결 → P3 소액 실거래. 이후 바이비트 → 바이낸스(서브계좌 완료 후) 순. 실행 대상 코인은 M1 데이터로 재선정 (PROM·ONG의 OKX 레그 유효성 포함) | 대기 |

주의: 같은 김프가 해외 3곳에 동시에 보이므로 **기회 중복 집계 금지** — 가상 수확·리스크 한도는
(코인, 국내) 단위 한 슬롯으로 센다(자본은 하나다). 실행 시 해외 거래소 선택은 "그 레그의 순엣지
최대"가 기준(수수료·출금비 차이 반영)이며, 이는 M1 데이터가 알려준다.

---

## 7. 미결 논제 (브레인스토밍 큐)

★ = 이게 틀리면 나머지가 무의미 / ✅ = 확정되어 본문 승격

### 열린 질문 (확정 논제의 세부)
- ~~O1. 김프 방향 헤지~~ → ✅ §1.2 기본 naked + 가드레일 3종 (조건부 헤지는 config로 구현, 기본 off)
- ~~O2. 리밸런싱 실행 정책~~ → ✅ §1.1 기회 주도 복귀 레그 + 밴드/타임아웃 (파라미터는 P1 데이터로)
- ~~O3. DEX 유닛 구성~~ → ✅ 체인별 1:1:1, 필요시 조정 (§4.2). 가스/스테이블 비율은 T5에서
- **O4. 헤지 증거금 규모**: 동시 in-flight 상한이 정해지면(T13) 도출

### 전략 코어
- ~~★ T1. 루프 폐쇄~~ → ✅ §1.1 목표 배분 상시 복원 모델
- ~~★ T2. 전송 레그 헤지~~ → ✅ §1.2 (역프 확정, 김프 방향은 O1)
- **★ T3. 코인 유니버스 매트릭스** (논의 중 — 제안 정리됨):
  - 평가 차원: ①양측 상장(국내 KRW × 해외 USDT 현물) ②실효 전송시간 = 블록타임 × 도착지 요구 확정수(V4) ③출금 수수료 ④양측 호가 깊이(운용 금액 대비) ⑤해외 무기한 유무(naked 한도 배정에 사용) ⑥입출금 안정성 이력 ⑦멀티체인 코인의 최적 네트워크 ⑧**자산 동일성 검증 통과 (V7 — 티커 충돌 배제, 거래 유니버스의 하드 게이트)**
  - 구조: **레일 = USDT 기본** (운용자 확정 — 저비용 네트워크로 "USDT 사서 출국"이 표준 경로. XRP·TRX는 USDT 김프가 불리할 때의 대안 레일) + **기회 코인**(김프 분산 큰 알트 — 수익 원천)
  - **운용자 실전 사전정보 (2026-08-02)**: 수동 시절 수익 코어 = **저시총 + 원화상장 신규 코인**. 그리고 **거래량 폭발 레짐에서는 1~2% 엣지도 신뢰 가능**(깊이가 받쳐줘서 무조건 체결) → 반영: ①유니버스는 넓게 수집(국내 KRW 전 종목 ∩ 바이낸스) ②신규 KRW 상장 감지 알림 ③거래량 레짐 태깅(P1에서 "고회전 시 엣지 신뢰도" 검증) ④신규 상장 코인은 화이트리스트 등록 리드타임·초기 입출금 불안정을 감안해 T4 게이트 강화 대상
  - 원칙: **유니버스 확정은 P0 실측이 한다** — 2주+ 데이터에서 코인별 실행김프 분산·기회 빈도·지속시간 상위 코인을 뽑고 운영 필터(①~⑦) 통과분만 채택. 화이트리스트 (코인,네트워크,목적지) 조합 등록 체크리스트는 P2 진입 조건
- **★ T4. 함정 탐지** (논의 중 — 제안 정리됨):
  - 급등 원인 분류: ①입금 정지(지갑 점검 — 최다 함정) ②네트워크 혼잡/정지 ③상장빔·국내 단독 수급 ④글로벌 급변동 중 가격 발견 시차(진짜 기회일 수 있음) ⑤코인 이벤트(스냅샷·하드포크)로 입출금 정지
  - **⑥ 인위적 펌핑 함정 (운용자 실전 경험, 2026-08-02)**: 세력이 국내가를 급등시켜 김프를 만들고, 재정거래꾼이 해외 매수→전송하는 동안 유지하다가 **도착 직전 덤핑**해 물량을 넘김. 재정거래 매수 유입으로 해외가도 끌려 올라간 상태라 naked 포지션은 김프 소멸+가격 하락을 같이 맞음
  - **게이트·방어 (v6 재설계 — §1.4 속도 원칙 적용: 시간을 기다리는 게이트 전면 금지)**:
    - ~~숙성 필터(엣지 X분 지속 후 진입)~~ **폐기 (2026-08-02 운용자 지적)** — 대기는 기회 상실을 넘어 **역선택**임: 지속된 프리미엄만 골라 진입 = 이미 출발한 경쟁 물량이 도착하기 직전, 즉 **대열의 꼬리**로 들어가는 행위. 함정이 무는 바로 그 자리
    - ~~분할 진입(카나리 사이클 완주 후 증액)~~ **폐기 (2026-08-02 운용자 지적, 같은 논리)** — 증액분이 한 사이클 늦게 = 대열 꼬리로 진입. 사이즈는 진입 순간에 한 번, 사전 증거(신뢰 등급)로 결정하고 끝
    1. **0-지연 품질 게이트** (전부 스트리밍 데이터에서 밀리초 평가, 임계 경로 지연 0):
       ①지갑 상태 캐시 (출발지 출금 가능 ∧ 도착지 입금 가능 — 폴링은 백그라운드, 판단은 캐시).
         **미확인 = 거래 불가** (보수 원칙, 2026-08-16 확정): 지갑 상태를 모르는 코인은 엣지가 커도 거래 유니버스에서 제외.
         실측 근거: 가동 중 스냅샷에서 **빗썸 50/149·업비트 43/150종이 입출금 정지** — 유니버스의 약 1/3이 함정 지대이며,
         김프 사이트들이 보여주는 "기회"의 상당수가 이 구역임. 빗썸 구 public API는 신규 상장(예: AI) 미포함 커버리지 갭 실측 → P2에서 보강
       ②깊이 반영 왕복 엣지 (VWAP 엔진이 자동 수행 — 얇은 스파이크는 여기서 소거)
       ③업비트·빗썸 동시성 (단일 거래소만 급등 = 의심 가중)
       ④해외가 단기 모멘텀 (해외가가 김프와 동반 급등 = 함정 확률·naked 분산 최대 신호)
       ⑤거래량 동반 여부 (운용자: 거래량 폭발 레짐 = 유기적, 신뢰 가능)
    2. **신뢰 등급 사이즈** (대기가 아니라 즉시 결정): 코인·패턴별 실적 기반 등급 → 검증된 코인은 즉시 풀사이즈, 신규·의심 신호는 즉시 소액. "작게라도 지금 간다"
    3. **사이즈 규율**: 사이클 명목 ≤ 해당 코인 최근 회전율의 x% — 당해도 탈출 슬리피지가 상한
    4. **도착 시점 재평가 플레이북**: 도착 때 엣지 소멸 시 **투매 금지** — 패시브 청산 / 타 국내 거래소 라우팅 / 보유 대기. 함정 손실의 대부분은 무너진 호가에 시장가 투매에서 발생
    5. **의심 결합 시 병렬 헤지**: 게이트 ③④ 의심 신호 결합 시 그 사이클만 O1 헤지 on — 숏 주문은 매수와 **병렬 발사**라 임계 경로 지연 없음 (덤핑 시 숏이 손실 상쇄)
    6. **궁극의 방어 = 속도 자체 (§1.4)**: 함정은 늦게 도착하는 물량을 문다. 최속 포착→매수→출금→매도가 방어의 본체이고, 위 1~5는 그 위의 안전벨트
    - 사이클 중 입출금 정지 대응은 T6 타임아웃 정책 + CRIT로 (변경 없음)
- **T5. DEX 역할**: 해외 레그 조달? 우회 경로? 브릿지? MEV·가스 비용 모델. (보안은 §4.2로 확정, 역할 정의가 미결)

### 실행 계층
- **★ T6. 사이클 상태머신·장애 복구** (v7 제안 — 논의 중):
  - 사이클 타입 3종: INBOUND(김프) / OUTBOUND(역프, 헤지 병렬) / REBALANCE(복귀 레그, 게이트 없음)
  - 상태 흐름 (INBOUND 기준, 전 전이 타임스탬프+영속화, **부수효과 전 기록 = write-ahead**):
    `SIGNAL → BUY_PLACED → BUY_FILLED (∥ HEDGE_FILLED) → WITHDRAW_REQUESTED → WITHDRAW_PROCESSING(최장 단계, 실측 p95 타임아웃) → CHAIN_CONFIRMED → DEPOSIT_CREDITED → SELL_PLACED → SELL_FILLED (∥ HEDGE_CLOSED) → SETTLED`
  - 이벤트 소스: 체결·입금은 프라이빗 WS(§1.4), 출금 상태는 REST 폴링(레이트리밋 내)
  - 예외 상태: ABORTED(출금 전 실패 → 즉시 반대매매 청산) / STUCK_WITHDRAWAL(처리 타임아웃 → CRIT + 취소 가능 여부별 플레이북) / STUCK_DEPOSIT(브로드캐스트 후 미크레딧 → CRIT) / 부분체결은 체결 수량 기준으로 사이클 계속(최소 수량 미달 시 ABORT)
  - 영속화: SQLite(WAL) 단일 파일 — cycle_id별 전이 로그 append + 멱등키(client_order_id=f(cycle_id, leg), 출금 uuid 선기록 후 발사)
  - 복구: 재기동 시 열린 사이클 로드 → 거래소 실상태(주문·출금·잔고) 대조 → 해당 상태부터 리스너 재개
  - L1 킬스위치는 SIGNAL만 차단, in-flight 사이클은 SETTLED까지 완주 (§1.3 정합)
  - **확정 (2026-08-02)**:
    ① 부분체결 최소 수량 임계 = **목표의 10%** (운용자 확정). 10% 이상 체결이면 그 수량으로 사이클 계속, 미만이면 ABORT(즉시 반대매매)
    ② STUCK_WITHDRAWAL 플레이북 (운용자 경험: **빗썸 = 즉시 취소 가능, 업비트 = 비결정적**):
       타임아웃 도달 → 취소 시도 → 성공 시 자금 복귀·재라우팅(다른 코인/경로) / 실패 시 연장 타임아웃으로 전환하고 **naked 사이클이면 보호 헤지 자동 발동** (지연된 전송 = 연장된 노출이므로)
    ③ 잔고 대조(reconciliation) 4단: 기동 시 전체 대조 → 사이클 정산 시 관련 자산 대조 → 백그라운드 5분 주기 경량 대조 → 일일 감사 리포트(텔레그램, 손익+드리프트). 불일치 소액은 거래소 값 채택+로그, 임계 초과는 CRIT+L1
- **T7. 실행 전술**: IOC vs 지정가, 최대 슬리피지 가드, 부분 체결 시 헤지 동기화, 레그 실패 롤백.
- **T8. 고시환율 소스 선정** (이원 김프 구조는 §2로 확정) — 실측: 던어무 엔드포인트가 EC2에서 DNS 실패 (2026-08-02) → 폴백 체인(던어무→er-api 일간) 구현. 정식 실시간 소스는 계속 논제 (실행 김프는 무영향이라 급하지 않음)

### 인프라 계층
- **T9. 배치**: 운용 서버 확보됨 — **기존 AWS t3.large (타 워크로드와 공유)**. P0 수집기 배포 대상. 남은 논제: 고정 IP(API 화이트리스트), 해외 거래소 클라우드 IP 정책, 스케일업 시 전용 인스턴스 분리.
- **T10. 데이터 계층**: 인메모리 핫패스 / TimescaleDB+Parquet 콜드패스, 호가 15단.
- ~~T11. 키 보안~~ → ✅ §4 보안 아키텍처
- **T12. 관측성**: NTP 동기화, 스트림 신선도 감시, Prometheus/Grafana.
- **T17. 서명 격리 (신규)**: $1M 단계의 DEX 키 관리 — 서명 데몬 분리, MPC/HSM 검토. §4.2

### 운영·리스크 계층
- **★ T13. 리스크 한도** — ✅ **확정 (운용자 승인 2026-08-28)**. $30k 테스트 단계 수치:

  | 한도 | 제안치 | 비고 |
  |---|---|---|
  | 사이클당 명목 | **$2,000** | 실제 사이즈 = min(이 한도, 엔진 capacity). 수업료가 싼 크기 + 고정비(출금비)가 엣지를 안 잡아먹는 하한 |
  | 동시 in-flight 총액 | **$6,000** | 총자본의 20%. 전송 중 = 최대 노출 상태 |
  | 코인당 in-flight | **$3,000** | 함정 폭발 반경 제한 — 한 코인에 몰빵 전송 금지 |
  | 시간당 신규 사이클 | **10건** | 버그 폭주 방지 (초기 P3만, 안정화 후 상향) |
  | 일일 실현손실 | **−$300 (1%)** | 도달 시 자동 L1 + CRIT |
  | 연속 실패 사이클 | **3회** | ABORT·타임아웃 연속 3회 → 자동 L1 |
  | STUCK 사이클 | 1건: 해당 코인 신규 차단 / 2건 누적: L1 | |
  | 리밸런싱 출금 | 전 건 텔레그램 승인 (§4.1 방어선 5 기확정) | |

  모든 수치는 config — 무사고 이력에 따라 상향. O4(헤지 증거금)는 in-flight $6k 기준 ~$3k면 충분(3x 기준 버퍼 포함).
- **T14. 회계 원장**: 복식부기, 사이클별 손익 분해(스프레드/펀딩/수수료/슬리피지/환율), 세무 원천 자료.
- **T15. 확장 시 깨지는 것**: 일일 출금 한도, 알트 깊이 대비 주문 규모, 다계좌·법인·세무 구조.
- **T16. 백테스트 시뮬레이터**: P0 데이터로 헤지 기대효과·기회 분포 정량화.

---

## 8. 리스크 레지스터

| 리스크 | 심각도 | 대응 |
|---|---|---|
| 전송 중 김프 급락 | 높음 | §1.2 헤지 (역프 완전 락, 김프 방향 O1) |
| 사이클 중 입출금 중단 | 높음 | T4 게이트 + 중단 시 대응 절차 |
| 외국환거래법 개정 시행 (≈11월) | 높음 | 시행령 모니터링, 확대 전 전문가 확인 |
| 봇 크래시 중 자산 in-flight | 높음 | T6 영속 상태머신 + 데드맨 경보 |
| DEX 핫키 유출 (전액 탈취 가능 유일 지점) | 높음 | §4.2 배분 상한·키 분리·approve 최소화, T17 |
| 서버 장악 → 악성 거래로 가치 유출 | 중간 | §4.1 잔여 리스크 대응 (주문 상한·서킷브레이커) |
| 스트림 조용한 죽음 (stale 시세로 판단) | 중간 | T12 신선도 감시, stale 시 진입 차단 |
| API 키 유출 | 낮음 (방어 후) | §4.1 5중 방어 — 화이트리스트가 최종 방어선 |
| USDT 디페그 | 낮음 | 실행 김프 기준 계산이라 부분 흡수, 극단 시 L2 |
